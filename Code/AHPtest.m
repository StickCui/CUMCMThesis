%AHP 层次分析法测试程序
clear; clc;
%a=[1 2 8;1/2 1 6;1/8 1/6 1]; %测试判断矩阵
%以下是判断矩阵【值越大越重要】
%a=[1 1 1/2 1/3;1 1 1/2 1/3;2 2 1 2/3;3 3 3/2 1];
a=[1 3/2 3 3/4;2/3 1 2 1/2;1/3 1/2 1 1/4;4/3 2 4 1];

[p,q]=eig(a);%p 特征列向量，q 特征值对角矩阵

[Q,point]=max(max(q));%取最大特征值Q ，以及其对应位置point

W=p(:,point);%求出特征向量W
w=W/sum(W);%求出权值向量w

[~,n]=size(a);
RI=[0 0 0.58 0.90 1.12 1.24 1.32 1.41 1.45 1.49 1.51];
CI=(Q-n)/(n-1);
CR=CI/RI(n)
